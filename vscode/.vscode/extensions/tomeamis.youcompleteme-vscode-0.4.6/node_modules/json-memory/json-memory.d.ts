declare module 'json-memory'{
	class JsonMemory
	{
		constructor(file: string, load: boolean)
		constructor(file: string, callback: (error: string|null, obj: JsonMemory) => any)

		_file: string

		read(callback: (error: string|null, obj: JsonMemory) => any): JsonMemory

		toString(): string

		write(callback: (error: string|null) => any): JsonMemory

	}

	export = JsonMemory
}
